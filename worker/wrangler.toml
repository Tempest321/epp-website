name = "fse-api-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

[vars]
ALLOWED_ORIGINS = "https://forsightengine.ai,https://forsightengine.com,https://forsightenginepage.pages.dev,http://localhost:8000"

# API key stored as secret, NOT in code
# Run: wrangler secret put GEMINI_API_KEY
